"use client";
import React from "react";

const HeadingAccent = ({ children }: { children: string }) => {
  return <h3 className="font-semibold text-sm mb-0">{children}</h3>;
};

export default HeadingAccent;
